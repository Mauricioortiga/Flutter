**Conhecendo as variáveis do Dart:**

main () {
  int a = 3;
  double b = 3.1;
  bool estaChovendo = true;
  bool estaFrio = false;
  var c = 'Você';
  print (c is String); - **Ele retorna como verdadeiro a variável "c".**

---

**Lista de nomes:**

main () {
  var nomes = ['Ana', 'Bia', 'Carlos');
  nomes.add('Daniel');
  nomes.add('Daniel');
  nomes.add('Daniel');
  print (nomes. length); - **Definir para a variável nomes**
  print (nomes.elementAt(0)); - **Sabemos o elemento 0 da lista.**
  print (nomes[5]); - **Entrega Daniel (Índice 5)**
}

**Lista set:**

main () {
  var conjunto = {0, 1, 2, 3, 4, 4, 4, 4};
  print (conjunto.length); - **Irá dizer que o conjunto há 5 elementos, porque o conjunto não aceita repetição.**
  print (conjunto is Set); - **Ele confere se essa lista é um Set.**

**Se eu colocar Set<int> - Significa que o Set só irá aceitar valores inteiros.**
---

**Estrutura de chave-valor:**

**O map é uma estrutura chave valor, neste caso: String - Double.**

main () [
  map<String, double> notasDosAlunos = {
    'Ana':9.7,
    'Bia':9.2,
    'Carlos':7.8,
  }

  ---

  **(Pegamos todas as chaves do Map com Keys)**
  
  for (var chave in notasDosAlunos.keys) {
  print ('chave = $chave');
  }

**Resultado:**

chave = Ana
chave = Bia
chave = Carlos
  
**Aqui fazemos a interpolação da String.**

---

for (var valor in notasDosAlunos.values) {
  print ('valor = $valor');

**Descobrir os tipos de variaveis de valor**

---

**Selecionamos aqui as Entradas de Map**
  
  for (var registro in notasDosAlunos.entries) { 
  print ('${registro.key} = ${registro.value}');

**Aqui irá mostrar a Chave e o Valor, depois Chave-Valor:**

chave = Ana
chave = Bia
chave = Carlos

valor = 9.7
valor = 9.2
valor 7.8

Ana = 9.7
Bia = 9.2
Carlos = 7.8

---

**O dynamic é uma variável dinâmica:**

dynamic x = 'Teste';
x = 123
x = false;

**Pode armazenar string, inteiro, boolean, etc.**

**Por padrão, iremos definir explicitamente o tipo de variável.**

---

**Criando constantes:**

main () {
  var a = 3;
  a = 4;

  final b = 3;

  // b = 6;

  const c = 5;
  c = 7;
}
